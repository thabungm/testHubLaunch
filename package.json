{
  "name": "test-hub-launch-contact",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "node --import tsx --test src/slack.test.ts",
    "contact": "tsx scripts/contact.ts",
    "test:contact": "tsx scripts/test-contact.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5"
  }
}
